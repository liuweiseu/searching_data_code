处理主要用到3个程序
1、data_no_baseV2是用于去基线的，会生成中间文件，后缀是nobase
2、TimeFreq_V2_3是最新的做周期叠加的程序，处理的是后缀为nobase的文件，生成新的中间文件，后缀为TimeFreq
3、BinProcessV2_1是做BIN叠加处理的，是将周期叠加后的数据，时间轴上合并成多个BIN画图（这样画图比较方便）

checkdata函数是检查丢包的函数，具体详见函数文件中的说明

TimeFreq程序更新说明：
1、V3.1版本更新了叠加算法，考虑了周期与时间分辨率不是整数倍关系的问题，将一帧数据按权重进行分配